node_modules/
dist/
.turbo/
coverage/
*.log
.env
.env.local
.DS_Store

# babysitter runtime state
.a5c/

# Scaffolded client shells — each is its own repo, not part of the framework monorepo
clients/
# ...except personal-email-os, which is Matt's own deployable install and
# lives in this repo so its config is version-controlled. Other clients
# (e.g. c-and-m-construction-os) stay out and get their own repos.
!clients/personal-email-os/
