{
  "name": "@frontrangesystems/business-os-agent-digest",
  "version": "0.0.2",
  "description": "Daily digest. Walks every active user, calls each registered module's digestContribution, sends one email per user via the email capability.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@frontrangesystems/business-os-agent-sdk": "workspace:^",
    "@frontrangesystems/business-os-connector-sdk": "workspace:^",
    "@frontrangesystems/business-os-db": "workspace:^",
    "drizzle-orm": "^0.33.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "publishConfig": {
    "access": "restricted",
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frontrangesystems/business-os.git",
    "directory": "agents/digest"
  }
}
